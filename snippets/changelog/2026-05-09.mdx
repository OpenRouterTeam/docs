### Product changes

- **Fixed: collapsible sections in chatroom responses** -- Assistant messages in the [chatroom](https://openrouter.ai/chat) once again render markdown-embedded HTML such as `<details>` / `<summary>` blocks and inline tags like `<b>`.
