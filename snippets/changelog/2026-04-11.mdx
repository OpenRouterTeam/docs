### Product changes

- **Ordered provider selection in presets** -- [Presets](/guides/features/presets) now show ordered provider selection with numbered badges and a summary, giving you more control over provider routing.
- **Video generation parameters** -- Video generation now supports `generate_audio`, `seed`, and `frame_images` parameters for more control over output.
