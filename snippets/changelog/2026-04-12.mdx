### Product changes

- **Blocked models for auto-router** -- You can now specify `ignored_models` to exclude specific models from auto-router selection, giving finer control over which models handle your requests.
- **Native passthrough fix** -- Fixed an issue where native passthrough was incorrectly inserting provider-specific tools into normalized requests.
