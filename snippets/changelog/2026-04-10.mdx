### Product changes

- **Regex prompt injection detection** -- Guardrails now support a regex-based prompt injection detection toggle in the editor.
- **Confirmation modals** -- Browser `confirm()` and `alert()` dialogs have been replaced with in-app confirmation modals for a more polished experience.
- **PDF parsing timeout** -- Increased PDF file parsing timeout to 120 seconds for larger documents.
