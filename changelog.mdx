---
title: "API Changelog"
description: "Changes to the OpenRouter API, generated from the OpenAPI specification on every release."
icon: clock-rotate-left
rss: true
---

Entries on this page are generated automatically from [OpenAPI specification](/api_reference/overview) diffs when releases ship. Breaking changes are reviewed by a human before publication — see [API Versioning](/api_reference/versioning) for what counts as a breaking change and how deprecations are announced. Endpoint paths are shown relative to the API base URL (`https://openrouter.ai/api/v1`).

{/* api-changelog:entries */}

<Update label="July 25, 2026" description="release-20260725-024445-robert-j-y-3f435a5, release-20260725-165514-ping-Toven-3c293e5, release-20260725-233254-yogasanas-c672824" tags={["BYOK","Breaking","Schemas","responses"]} rss="2 breaking changes, 2 modified endpoints, 5 modified schemas.">

## Breaking changes

- [`POST /responses`](/api/api-reference/responses/create-a-response) — tags `beta.responses` property removed
  - **Migration: no action for HTTP clients.** The Responses API is now GA. `beta.responses` was an OpenAPI grouping tag and SDK namespace, never a URL — `POST /api/v1/responses` and its request and response schemas are unchanged, so raw HTTP callers and OpenAI-compatible clients (including the OpenAI Agents SDK) need no changes. The endpoint now appears under **Responses** instead of **Beta responses** in the API Reference.
  - **Migration for SDK users: optional until sunset.** The canonical namespace is now `responses` — `client.responses.send()` (TypeScript, Python) and `client.Responses.Send()` (Go). The `beta.responses` namespace, and TypeScript's `betaResponsesSend` standalone function, keep working as deprecated aliases; their removal will be announced here with a sunset date before it ships.
- $.tags: beta.responses object removed
  - Same change as above at the specification level: the `beta.responses` tag definition is gone from `$.tags`.

## Modified endpoints

- [`POST /responses`](/api/api-reference/responses/create-a-response) — tags property_added
- [`GET /byok`](/api/api-reference/byok/list-byok-provider-credentials) — parameter `provider` updated

## Modified schemas

- `MessagesStartEvent` — property `provider` updated
  - Used by [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message)
- `ProviderName` — enum property_added

  <Expandable title="11 endpoints using this schema">
    - [`GET /endpoints/zdr`](/api/api-reference/endpoints/preview-the-impact-of-zdr-on-the-available-endpoints)
    - [`GET /models/{author}/{slug}/endpoints`](/api/api-reference/endpoints/list-all-endpoints-for-a-model)
    - [`POST /chat/completions`](/api/api-reference/chat/create-a-chat-completion)
    - [`POST /embeddings`](/api/api-reference/embeddings/submit-an-embedding-request)
    - [`POST /images`](/api/api-reference/images/generate-an-image)
    - [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message)
    - [`POST /presets/{slug}/chat/completions`](/api/api-reference/presets/create-a-preset-from-a-chat-completions-request-body)
    - [`POST /presets/{slug}/messages`](/api/api-reference/presets/create-a-preset-from-a-messages-request-body)
    - [`POST /presets/{slug}/responses`](/api/api-reference/presets/create-a-preset-from-a-responses-request-body)
    - [`POST /rerank`](/api/api-reference/rerank/submit-a-rerank-request)
    - [`POST /responses`](/api/api-reference/responses/create-a-response)
  </Expandable>
- `ProviderOptions` — new property `claude-on-aws`

  <Expandable title="4 endpoints using this schema">
    - [`POST /audio/speech`](/api/api-reference/tts/create-speech)
    - [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)
    - [`POST /images`](/api/api-reference/images/generate-an-image)
    - [`POST /videos`](/api/api-reference/video-generation/submit-a-video-generation-request)
  </Expandable>
- `ProviderResponse` — enum property_added
  - Used by [`GET /generation`](/api/api-reference/generations/get-request-&-usage-metadata-for-a-generation)
- `BYOKProviderSlug` — enum property_added

  <Expandable title="4 endpoints using this schema">
    - [`GET /byok`](/api/api-reference/byok/list-byok-provider-credentials)
    - [`GET /byok/{id}`](/api/api-reference/byok/get-a-byok-provider-credential)
    - [`PATCH /byok/{id}`](/api/api-reference/byok/update-a-byok-provider-credential)
    - [`POST /byok`](/api/api-reference/byok/create-a-byok-provider-credential)
  </Expandable>

</Update>

<Update label="July 24, 2026" description="release-20260724-191903-abhinav-pola-9b6ee52, release-20260724-201142-robert-j-y-89bfb99" tags={["Schemas"]} rss="8 modified schemas.">

## Modified schemas

- `MessagesStartEvent` — property `provider` updated
  - Used by [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message)
- `ProviderName` — enum property_added

  <Expandable title="11 endpoints using this schema">
    - [`GET /endpoints/zdr`](/api/api-reference/endpoints/preview-the-impact-of-zdr-on-the-available-endpoints)
    - [`GET /models/{author}/{slug}/endpoints`](/api/api-reference/endpoints/list-all-endpoints-for-a-model)
    - [`POST /chat/completions`](/api/api-reference/chat/create-a-chat-completion)
    - [`POST /embeddings`](/api/api-reference/embeddings/submit-an-embedding-request)
    - [`POST /images`](/api/api-reference/images/generate-an-image)
    - [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message)
    - [`POST /presets/{slug}/chat/completions`](/api/api-reference/presets/create-a-preset-from-a-chat-completions-request-body)
    - [`POST /presets/{slug}/messages`](/api/api-reference/presets/create-a-preset-from-a-messages-request-body)
    - [`POST /presets/{slug}/responses`](/api/api-reference/presets/create-a-preset-from-a-responses-request-body)
    - [`POST /rerank`](/api/api-reference/rerank/submit-a-rerank-request)
    - [`POST /responses`](/api/api-reference/responses/create-a-response)
  </Expandable>
- `ProviderOptions` — new property `voyageai`

  <Expandable title="4 endpoints using this schema">
    - [`POST /audio/speech`](/api/api-reference/tts/create-speech)
    - [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)
    - [`POST /images`](/api/api-reference/images/generate-an-image)
    - [`POST /videos`](/api/api-reference/video-generation/submit-a-video-generation-request)
  </Expandable>
- `ProviderResponse` — enum property_added
  - Used by [`GET /generation`](/api/api-reference/generations/get-request-&-usage-metadata-for-a-generation)
- `AnthropicCompactionBlock` — new property `encrypted_content`
  - Used by [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message)
- `MessagesToolAdditionBlock` — new property `cache_control`; new union variant added; type property_removed
  - `tool` widened from a single `tool_reference` object to a union of `tool_reference`, `mcp_tool_reference` (`name` + `server_name`) and `mcp_toolset_reference` (`server_name`). The `tool_reference` variant is unchanged, so existing `{ "type": "tool_reference", "name": "…" }` payloads stay valid — the `name` and `type` properties moved into that variant rather than being removed.
  - Used by [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message), [`POST /presets/{slug}/messages`](/api/api-reference/presets/create-a-preset-from-a-messages-request-body)
- `MessagesToolRemovalBlock` — new property `cache_control`; new union variant added; type property_removed
  - `tool` widened the same way as `MessagesToolAdditionBlock`; existing `tool_reference` payloads stay valid.
  - Used by [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message), [`POST /presets/{slug}/messages`](/api/api-reference/presets/create-a-preset-from-a-messages-request-body)
- `ORAnthropicStopReason` — enum property_added
  - Used by [`POST /messages`](/api/api-reference/anthropic-messages/create-a-message)

</Update>

<Update label="July 8, 2026" tags={["Schemas"]} rss="1 new schema, 1 modified schema.">

## New schemas

- `ImageGenTextChunkEvent`
  - Used by [`POST /images`](/api/api-reference/images/generate-an-image)

## Modified schemas

- `ImageStreamingResponse` — new union variant added
  - Used by [`POST /images`](/api/api-reference/images/generate-an-image)

</Update>

<Update label="July 7, 2026" tags={["Datasets","Images","Models","STT"]} rss="3 modified endpoints, 1 new response code, 3 new schemas, 6 modified schemas.">

## Modified endpoints

- [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription) — description updated; request body now accepts `multipart/form-data`
- [`GET /datasets/rankings-daily`](/api/api-reference/datasets/daily-token-totals-for-top-50-models):
  - description updated
  - new parameter `category`
  - new parameter `context_bucket`
  - new parameter `language_type`
  - new parameter `modality`
  - new parameter `period`
- [`GET /models`](/api/api-reference/models/list-all-models-and-their-properties):
  - new parameter `max_age_days`
  - new parameter `max_agentic_index`
  - new parameter `max_coding_index`
  - new parameter `max_intelligence_index`
  - new parameter `max_output_price`
  - new parameter `max_tool_success_rate`
  - new parameter `min_age_days`
  - new parameter `min_agentic_index`
  - new parameter `min_coding_index`
  - new parameter `min_intelligence_index`
  - new parameter `min_output_price`
  - new parameter `min_tool_success_rate`
  - description updated
  - parameter `sort` updated

## New response codes

- [`POST /images`](/api/api-reference/images/generate-an-image) — now returns `413`

## New schemas

- `STTSegment`
  - Used by [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)
- `STTTimestampGranularity`
  - Used by [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)
- `STTWord`
  - Used by [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)

## Modified schemas

- `ChatFunctionTool` — new union variant added
  - Used by [`POST /chat/completions`](/api/api-reference/chat/create-a-chat-completion), [`POST /presets/{slug}/chat/completions`](/api/api-reference/presets/create-a-preset-from-a-chat-completions-request-body)
- `FusionServerToolConfig` — description updated

  <Expandable title="4 endpoints using this schema">
    - [`POST /chat/completions`](/api/api-reference/chat/create-a-chat-completion)
    - [`POST /presets/{slug}/chat/completions`](/api/api-reference/presets/create-a-preset-from-a-chat-completions-request-body)
    - [`POST /presets/{slug}/responses`](/api/api-reference/presets/create-a-preset-from-a-responses-request-body)
    - [`POST /responses`](/api/api-reference/responses/create-a-response)
  </Expandable>
- `ImageGenCompletedEvent` — description updated; example modified
  - Used by [`POST /images`](/api/api-reference/images/generate-an-image)
- `ImageGenerationResponse` — property `media_type` updated
  - Used by [`POST /images`](/api/api-reference/images/generate-an-image)
- `STTRequest` — new property `response_format`; new property `timestamp_granularities`
  - Used by [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)
- `STTResponse`:
  - new property `duration`
  - new property `language`
  - new property `segments`
  - new property `task`
  - new property `words`
  - Used by [`POST /audio/transcriptions`](/api/api-reference/stt/create-transcription)

</Update>

<Update label="July 3, 2026" tags={["Workspaces"]} rss="1 new endpoint, 1 new schema.">

## New endpoints

- [`GET /workspaces/{id}/members`](/api/api-reference/workspaces/list-workspace-members) — List workspace members

## New schemas

- `ListWorkspaceMembersResponse`
  - Used by [`GET /workspaces/{id}/members`](/api/api-reference/workspaces/list-workspace-members)

</Update>
